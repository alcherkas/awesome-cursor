# Awesome Cursor – Agent instructions

## Docs site (GitHub Pages)

- **Sidebar sync**: When you add, remove, or rename any **H2 (`##`)** or **H3 (`###`)** heading in `cursor-learning-plan.md` or in another doc listed in `_sidebar.md`, update `_sidebar.md` so the sidebar matches. Use the same nesting and anchor links (e.g. `cursor-learning-plan.md#part-1-fundamentals`).
